export type StatusDot = "amber" | "green" | "blue";

/** Shared status-dot colors — used by Work-card chips and the hero lab panel. */
export const DOT_COLOR: Record<StatusDot, string> = {
  amber: "#FBBF24",
  green: "#4ADE80",
  blue: "#60A5FA",
};
